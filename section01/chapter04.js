// 1. 변수
let age = 17;
console.log(age);

age = 30;
console.log(age);

// 2. 상수(변하지 않는 것)
const birth = "2009.03.17";

// 3. 변수 명명규칙(네이밍 규칙)
// 3-1. $, _ 제외한 기호는 사용할 수 없다.
let $_name;

// 3-2. 숫자로 시작할 수 없다.
let na2me1;
let _2name;

// 3-3. 예약어를 사용할 수 없다.

// 4. 변수 명명 가이드
let salesCount = 1;
let refundCoumt = 1;
let totalSalesCount = salesCount - refundCount;
